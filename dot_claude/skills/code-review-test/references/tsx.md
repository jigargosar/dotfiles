# TSX Overlay (TEST)

> ✅ TSX overlay loaded successfully.

## TSX-Specific Checks (placeholder)
- All TypeScript checks apply (also load `typescript.md`)
- Component props exposing internal state
- Inline business logic in render functions
- Overly broad prop types
