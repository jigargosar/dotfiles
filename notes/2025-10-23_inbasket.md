# inbasket


- fix C:\Users\jigar\Documents\PowerShell\Microsoft.PowerShell_profile.ps1 to not use inclues, instead use actual command itself.
- chezmoi, git command failing, need to use --
  - perhaps add more learnings from using chezmoi, i.e. how to configure a diff command, the three way diff for webstorm sucks. So perhaps we could investigate. or just describe how webstorm can be used for chezmoi diffs.
  - chezmoi cat-config
  - perhaps some more commands that are useful
- create tool like pik, search and delete processes. currently in home dir fkill.ps1
- sync chezmoi tool is not using newlines to display checkmoi output, and deletions are not handled properly.
- sync-chezmoi script/module publish and blog, perhaps use gist.
- TIL: powershell pwsh
    - hardlinks
    - running scripts via . command
    - psroot prefiz
