# keepnote features and ideas

- some ability to delete files
- its easy to forget that kn + arg will create a file, ideally the file should be created only when saving.
    - or some ability to explore notes folder and delete file
    - can show notes path, open in explorer.
    - use file name and bat as a keepnote command to find and delete files. 
    - perhaps wait for now, let this idea percolate
- can we control fzf search, since its too finegrained and selects charactes, singe from multiple words.
- we could use shortcuts to change modes, file name only, default with content, perhaps a delete mode.
